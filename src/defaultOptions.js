const defaultOptions = {
    columnMaxWidth: 512,
    columnRedundancyWidth: 0,
    widthAlias: 'width',
    labelAlias: 'label',
    fieldAlias: 'field',
    frozenAlias: 'frozen'
};

export default defaultOptions;
